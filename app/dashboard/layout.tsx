export const metadata = {
  title: 'Dashboard - Insight Student',
  description: 'Student dashboard for tracking progress and accessing learning materials',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
